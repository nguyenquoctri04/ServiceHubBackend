services:
  redis:
    image: redis:8-alpine
    container_name: servicehub-redis
    restart: unless-stopped

    ports:
      - "6380:6379"

    volumes:
      - redis-data:/data

volumes:
  redis-data:
